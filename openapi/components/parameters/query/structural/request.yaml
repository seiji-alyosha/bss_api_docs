name: request
in: query
required: false
description: Keywords or Bible passasge references. Cannot be both. If a passage, the response format looks like the `reference` parameter response. If a keyword, the response format looks like the `search` parameter response.
example: John 3:16
schema:
  type: string
  default: (none)
