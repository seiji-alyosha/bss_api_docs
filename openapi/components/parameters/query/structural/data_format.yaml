name: data_format
in: query
required: false
description: >-
  Formats the structure of the endpoint response. Parameter values can either be:

  - **Passage** - For more informative responses

  - **Raw / minimal** - For more concise responses
schema:
  type: string
  default: passage
