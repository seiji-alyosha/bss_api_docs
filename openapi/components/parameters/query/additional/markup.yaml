name: markup
in: query
description: >-
  Formats the markup for Strongs numbers or red letters. 'None', or 'raw'. 'raw'
  returns the markup as stored in the database. {} = srongs numbers, [] = added
  words, ‹› = red letters (not <>). Default is 5.
schema:
  type: string
  default: none
