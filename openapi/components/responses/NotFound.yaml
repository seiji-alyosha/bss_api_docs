description: Book not found
content:
  application/json:
    schema:
      type: object
      properties:
        error:
          type: string
          description: >-
            An error message that tells users what happened and how to prevent
            the error.
          example: >-
            Sorry, we couldn't find a book with that id. Please try with a
            different id.
